# 算法（LeetCode）

- **主语言**：TypeScript（与工程侧统一）；网页答题时按官方支持选 JS/TS。
- 建议按周建子页，例如 `week01-binary-search.md`，只记：**题号、思路一句话、易错点、模板变体**。
- 默写模板可放 `snippets/*.ts`，不必与 LeetCode 提交格式一致，便于本地 `tsc --noEmit` 检查语法。
