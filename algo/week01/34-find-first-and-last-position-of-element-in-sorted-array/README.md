# 34. 在排序数组中查找元素的第一个和最后一个位置

- **链接**：[LeetCode 34](https://leetcode.cn/problems/find-first-and-last-position-of-element-in-sorted-array/)
- **难度**：中等

给定升序数组 `nums` 与 `target`，返回 `target` 的起止下标；不存在则 `[-1, -1]`。

## 本地代码

见 [`solution.ts`](./solution.ts) 等；`npm run test` 含本题 Vitest / unittest / `cargo test`。
