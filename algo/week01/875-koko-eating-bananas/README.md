# 875. 爱吃香蕉的珂珂（Koko Eating Bananas）

- **链接**：[LeetCode 875](https://leetcode.cn/problems/koko-eating-bananas/) ⭐
- **难度**：中等
- **标签**：数组、二分查找

## 题目描述

珂珂喜欢吃香蕉。这里有 `n` 堆香蕉，第 `i` 堆有 `piles[i]` 根。

警卫已经离开了，将在 `h` 小时后回来。珂珂可以决定她吃香蕉的速度 `k`（单位：根/小时）。每个小时，她选择一堆香蕉，从中吃掉 `k` 根。如果这堆香蕉少于 `k` 根，她将吃掉这堆的所有香蕉，然后这一小时内**不会再吃别的堆**。

返回她能在 `h` 小时内吃完所有香蕉的**最小速度** `k`。

## 示例

```text
输入：piles = [3,6,7,11], h = 8
输出：4
解释：速度 k=4 时，每堆耗时 ceil(3/4)+ceil(6/4)+ceil(7/4)+ceil(11/4) = 1+2+2+3 = 8 ≤ 8
```

```text
输入：piles = [30,11,23,4,20], h = 5
输出：30
解释：只有 5 小时，5 堆，每堆最多 1 小时，速度必须 ≥ 最大堆 = 30
```

## 约束

- `1 <= piles.length <= 10^4`
- `piles.length <= h <= 10^9`
- `1 <= piles[i] <= 10^9`

## 本地代码

见 [`solution.ts`](./solution.ts)；解题思路见 [`思路.md`](./思路.md)。
